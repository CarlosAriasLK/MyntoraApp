import 'package:flutter/material.dart';

class ProgramasFormacionScreen extends StatelessWidget {
  const ProgramasFormacionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Programas de formacion'),
      ),
    );
  }

}